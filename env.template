# ============================================
# Database Configuration (AWS Aurora PostgreSQL Serverless v2)
# ============================================
# Option A: Set DATABASE_URL directly (must include sslmode=require; connection_limit=5 is added automatically if missing).
# Option B: Set AWS_POSTGRES_* below; DATABASE_URL is built from them (connection_limit=5 and sslmode=require are added).
# Do NOT hardcode credentials. Use Vercel / AWS Secrets Manager in production.

# Direct connection string (optional if AWS_POSTGRES_* are set):
# DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

# AWS Aurora / RDS PostgreSQL (used when DATABASE_URL is not set):
AWS_POSTGRES_HOST="your-cluster.cluster-xxx.us-east-1.rds.amazonaws.com"
AWS_POSTGRES_PORT="5432"
AWS_POSTGRES_DB="postgres"
AWS_POSTGRES_USER="postgres"
# Password: set if you have it. Leave empty to use IAM authentication (no RDS password needed).
# For IAM: enable IAM auth on the Aurora cluster and set AWS_REGION (or use same region as host).
# Vercel Aurora integration may inject credentials via AWS_ROLE_ARN.
AWS_POSTGRES_PASSWORD=""
# Optional for IAM auth: AWS_REGION (e.g. us-east-1). Inferred from host if unset.
# AWS_REGION="us-east-1"

# ============================================
# JWT Authentication Secrets
# ============================================
# Generate strong random strings (min 32 characters)
# Use: npm run generate-secrets
JWT_ACCESS_SECRET="your-super-secret-access-token-key-min-32-chars-change-this"
JWT_REFRESH_SECRET="your-super-secret-refresh-token-key-min-32-chars-change-this"

# ============================================
# Application URLs
# ============================================
# Frontend URL (for CORS and email links)
CLIENT_URL="http://localhost:3000"

# ============================================
# Email Configuration
# ============================================
# Email service: 'console', 'sendgrid', 'mailgun', 'smtp', or 'zoho'
# Use 'zoho' or 'smtp' for Zoho Mail
EMAIL_SERVICE="zoho"

# For SendGrid
SENDGRID_API_KEY=""

# For Mailgun
MAILGUN_API_KEY=""
MAILGUN_DOMAIN=""

# For SMTP / Zoho Mail
# Zoho Mail SMTP settings:
SMTP_HOST="smtp.zoho.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER="info@skin1stbeauty.com"
SMTP_PASS="YOUR_ZOHO_APP_PASSWORD_HERE"

# Email sender address (must match SMTP_USER for Zoho)
EMAIL_FROM="info@skin1stbeauty.com"
EMAIL_REPLY_TO="info@skin1stbeauty.com"
SUPPORT_EMAIL="info@skin1stbeauty.com"

# Company information (for invoices and PDFs)
COMPANY_EMAIL="info@skin1stbeauty.com"
COMPANY_PHONE="+233540599282"
COMPANY_WEBSITE="www.skin1stbeauty.com"

# ============================================
# WhatsApp Integration
# ============================================
# WhatsApp webhook URL for notifications
WHATSAPP_WEBHOOK_URL=""

# ============================================
# Frontend Environment Variables
# ============================================
# WhatsApp contact number (format: +233540599282 for Ghana)
REACT_APP_WHATSAPP_NUMBER="+233540599282"

# Phone numbers for calls (format: +233540599282 for Ghana)
REACT_APP_PHONE_NUMBER="+233540599282"
REACT_APP_PHONE_NUMBER_2="+233539064833"

# API URL (use /api for same domain, or full URL for domain)
REACT_APP_API_URL="/api"

# ============================================
# Image Upload (Cloudinary)
# ============================================
# Optional: For image uploads
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""

# ============================================
# Error Tracking (Optional)
# ============================================
# Sentry DSN for error tracking
SENTRY_DSN=""

# ============================================
# Node Environment
# ============================================
NODE_ENV="development"
